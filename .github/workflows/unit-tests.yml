name: Unit Tests

on:
  push:

jobs:
  build:
    name: Build executable
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - uses: ilammy/setup-nasm@v1

    - name: Build Pestilence
      run: make Pestilence

    - name: Upload Pestilence
      uses: actions/upload-artifact@v4
      with:
        name: Pestilence
        path: ./Pestilence

  no_folder_testempty_test:
    name: Test without test folders
    runs-on: ubuntu-latest
    needs: build

    steps:
    - uses: actions/download-artifact@v4
      with:
        name: Pestilence
        path: .

    - name: Make pestilence executable
      run: chmod +x ./Pestilence
    
    - name: Test without test folders
      run: ./Pestilence

  empty_folder_testempty_test:
    name: Test with empty test folders
    runs-on: ubuntu-latest
    needs: build

    steps:
    - uses: actions/download-artifact@v4
      with:
        name: Pestilence
        path: .

    - name: Make pestilence executable
      run: chmod +x ./Pestilence

    - name: Test with empty test folders
      run: mkdir -p /tmp/test && mkdir -p /tmp/test2 && ./Pestilence
