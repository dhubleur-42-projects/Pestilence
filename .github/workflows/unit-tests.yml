name: Unit Tests

on:
  push:

jobs:
  build:
    name: Build executable
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build Pestilence
      run: make Pestilence